import React from 'react';
import ReactDOM from 'react-dom';

it('can render empty settings state', () => {
    const div = document.createElement('div');
    //ReactDOM.render(<App />, div);
    ReactDOM.unmountComponentAtNode(div);
});
